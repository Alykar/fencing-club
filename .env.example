POSTGRES__HOST=localhost
POSTGRES__PORT=5432
POSTGRES__USER=fencing
POSTGRES__PASSWORD=password
POSTGRES__DB=fencing_club

# Must be at least 32 characters long
JWT__SECRET=change-me-in-production-use-long-random-string-here
JWT__ALGORITHM=HS256
JWT__ACCESS_TOKEN_TTL_SECONDS=3600
JWT__REFRESH_TOKEN_TTL_SECONDS=604800

HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info
# Set to true in production (requires HTTPS)
SECURE_COOKIES=false
